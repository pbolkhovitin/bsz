# AGENTS.md — Сеть 172.17.100/101/102/106.0/24 (проект bsz)

Документация и управление сетью: инвентаризация, топология, миграция на новое адресное
пространство, мониторинг, безопасность. Проект ведётся по образцу `projeckt-kg`.

## Статус проекта (текущий)

1. **Инвентаризация старой сети** 192.168.40.0/24 (108 хостов) + 192.168.41.0/24 (123 хоста) — **выполнена**.
2. **Миграция на новое адресное пространство** 172.17.0.0/16 — **в процессе**:
   - 172.17.100.0/24 — серверы и технические устройства
   - 172.17.101.0/24 — резерв/дополнительно
   - 172.17.102.0/24 — локальная сеть (рабочие станции, принтеры)
   - 172.17.106.0/24 — камеры и система безопасности
3. **Маршрутизатор:** MikroTik **RB5009** (заменил старый шлюз), DHCP на 172.17.102.0/24.
4. **Поиск коммутаторов D-Link** по MAC — **в работе** (найдены MikroTik, ищем D-Link).
5. **SNMP/LLDP** — настройка на коммутаторах по методологии projeckt-kg.

## Рабочий процесс (обязательно)

- **Каждый значимый шаг** фиксировать в `process-log.md` (журнал процесса), затем
  **коммитить и пушить** в `master`. Пользователь требует это после каждой операции.
- Документация — **на русском**, код/комментарии — **на английском**.
- Секреты **никогда** не коммитить (см. `.gitignore`).

## Структура репозитория

| Путь | Назначение |
|------|-----------|
| `README.md` | Обзор, навигация, сводка |
| `AGENTS.md` | Этот файл — инструкции для агента |
| `inventory.md` | Инвентаризация устройств (MAC/IP/локации) |
| `inventory-switches.md` | Матрица коммутаторов: модель, прошивка, CLI, методы доступа, SNMP |
| `topology.md` | Физическая топология (LLDP, связи) |
| `vlan.md` | VLAN-структура |
| `ipam-bsz.md` | Целевая IPAM-схема (172.17.0.0/16) |
| `ipam-roadmap.md` | Роадмап миграции (поэтапно) |
| `security.md` | Безопасность: SNMP, открытые сервисы |
| `config-recommendations.md` | Настройка SNMP/LLDP по моделям |
| `wifi.md` | Wi-Fi: точки доступа |
| `netbox.md` | Интеграция с NetBox |
| `zabbix.md` | Мониторинг |
| `process-log.md` | Журнал процесса (все действия) |
| `scripts/` | Скрипты: сканирование, NetBox-импорт, SNMP-перевод |
| `data/` | Данные инвентаризации (CSV) |
| `maps/` | Графическая карта (Mermaid) |
| `scan/` | Сырые данные сканирования (regeneratable) |
| `reports/` | Отчёты |
| `reglamenty/` | Регламенты |
| `backup/` | Бэкапы (gitignored) |

## Инфраструктура

### Текущая адресация (миграция)

| Подсеть | Назначение | Статус |
|---------|-----------|--------|
| 172.17.100.0/24 | Серверы/технические | 1 хост (сканер .254), шлюз .1 |
| 172.17.101.0/24 | Резерв | пусто |
| 172.17.102.0/24 | Локальная сеть | **98 хостов (DHCP от RB5009)**, доступ через шлюз |
| 172.17.106.0/24 | Камеры/безопасность | 1 хост (сканер .254), шлюз через 172.17.100.1 |

### Сетевое оборудование (найдено)

| Устройство | Адрес | MAC | Примечание |
|------------|-------|-----|------------|
| MikroTik RB5009 (шлюз/DHCP) | 172.17.102.1 | 04:F4:1C:65:27:EE | заменил старый шлюз, порты управления закрыты |
| MikroTik CRS328-4C-20S-4S+ | (192.168.88.1 LLDP) | 04:F4:1C:AC:8E:21 | коммутатор, RouterOS 7.19.5, порт combo3 |

### Методы доступа к коммутаторам (из projeckt-kg)

- **D-Link полный CLI** (Telnet `#`): `create snmp community <name> view <view> <ro|rw>`
- **D-Link упрощённый CLI** (`>`): нет SNMP-команд → веб
- **D-Link веб bootstrap (RPC)**: `dlink_community_rpc.py` (jsencrypt/RSA)
- **D-Link веб EXCU_SHELL**: `snmp-server community <name> <ro|rw>`
- **MikroTik**: MAC-telnet (mactelnet), Winbox, API (read-only)

## Ключевые скрипты

| Скрипт | Назначение |
|--------|-----------|
| `scripts/netbox_import_bsz.py` | Импорт инвентаря в NetBox |
| `scripts/change_snmp_community.sh` | Telnet CLI смена community |
| `scripts/dlink_community_rpc.py` | Веб-RPC смена community |
| `scripts/enable_lldp.sh` | Включение LLDP |

## Сканирование

### Команды

| Этап | Команда |
|------|---------|
| Discovery | `nmap -sn 172.17.<s>.0/24 -oG scan/raw/discovery_172_<s>.gnmap` |
| Порты/службы | `nmap -sS -sV --version-light -T4 -iL <список>` |
| SNMP (UDP 161) | `nmap -sU -p 161 --open -T4 -iL <список>` |
| LLDP | `lldpd` + `lldpcli show neighbor` |

### Рабочая станция сканирования
- Fedora (`tpt14g1-fedora`), nmap 7.92, mactelnet, lldpd
- Интерфейс `enp5s0`: 172.17.100.254/24, 172.17.102.254/24, 172.17.106.254/24 (+ 192.168.40.254/24, 192.168.41.254/24)
- Маршрут по умолчанию: через 172.17.100.1 (MikroTik RB5009)
- Все три новые подсети прописаны на интерфейсе — прямой доступ для сканирования

## Проблемы сети (справочно)

- Коммутаторы D-Link ещё не найдены в 172.17.0.0/16 (возможно, ещё не переведены или
  неуправляемые). Поиск продолжается.
- У MikroTik RB5009 закрыты все порты управления (22/80/443/8291/8728) — доступ только
  по MAC-telnet или через консоль.
- Камеры Dahua (40:7A:A4) не отвечают на SNMP community `public` — свои community.